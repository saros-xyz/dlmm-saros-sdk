export enum MODE {
  TESTNET = 'testnet',
  DEVNET = 'devnet',
  MAINNET = 'mainnet',
}

export enum LiquidityShape {
  Spot = 'Spot',
  Curve = 'Curve',
  BidAsk = 'BidAsk',
}

export enum RemoveLiquidityType {
  All = 'All',
  BaseToken = 'BaseToken',
  QuoteToken = 'QuoteToken',
}
